<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.whut.ruansong.musicplayer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:logo="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="edu.whut.ruansong.musicplayer.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="edu.whut.ruansong.musicplayer.DisplayActivity"></activity>
        <activity android:name="edu.whut.ruansong.musicplayer.DetailMusicPlay"></activity>
        <activity android:name="edu.whut.ruansong.musicplayer.MainInterface"></activity>
        <service android:name="edu.whut.ruansong.musicplayer.MusicService"
                  android:exported="true"></service>
    </application>

</manifest>